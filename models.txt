# Ollama models to pull on first startup (one per line, # for comments)
# !!!Be careful if you replace the embeddings model, different models ingest embeddings differently!!!
nomic-embed-text
qwen2.5:0.5b
